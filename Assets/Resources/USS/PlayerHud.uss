.container {
    flex-direction: column;
    bottom: 0;
    position: absolute;
    color: rgb(255, 255, 255);
    margin-left: 16px;
    margin-bottom: 16px;
}

.label {
    flex-grow: 1;
    background-color: rgba(0, 0, 0, 0);
    flex-direction: row;
    align-items: center;
}
